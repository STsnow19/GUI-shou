# 角模块移动机械臂项目进度

## 1. 项目目标

面向上肢运动能力受限人群，设计一套由穿戴式 IMU 粗略控制、视觉辅助精确抓取的移动机械臂系统。用户只需控制机器人靠近目标并表达抓取意图，视觉和机器人控制系统负责目标定位、对准和抓取修正。

## 2. 当前系统构想

```text
前臂 IMU → 用户意图 → 底盘/机械臂分时控制
相机 + YOLO + 深度 → 目标位置 → 视觉辅助对准和抓取
```

- 底盘：带悬挂的独立转向驱动角模块。
- 转向电机：DM4310（待根据实际资料确认具体型号和协议）。
- 驱动电机：DM3519（待确认具体型号和协议）。
- 机械臂：计划使用 DM 系列电机，自由度暂未确定，初步考虑 5～6 自由度。
- 底层控制器：STM32。
- 穿戴式传感器：前臂 IMU，计划使用 BNO085/BNO086 或同类姿态传感器。
- 视觉部分：师妹负责相机、目标检测和目标三维位置估计。
- ROS 2：暂不作为当前第一阶段任务，待底层电机、角模块和机械臂跑通后学习和接入。

## 3. 分工

### 本人负责

- STM32 底层程序；
- DM 电机 CAN 通信；
- 电机使能、反馈、限位和安全保护；
- 角模块转向和驱动闭环；
- 四模块底盘运动学与控制；
- 机械臂结构、关节控制和运动学；
- IMU 到底盘/机械臂指令的映射；
- 与视觉、上层控制的接口联调。

### 师妹负责

- 相机驱动；
- YOLO 目标检测；
- 深度和目标三维坐标估计；
- 视觉辅助对准和抓取相关算法。

## 4. 当前认识

角模块本身暂不作为主要理论创新点，而是作为便于搭建、维护和扩展的移动底盘基础。论文重点考虑放在：

1. 面向能力受限用户的低精度 IMU 遥操作；
2. IMU 粗略意图与视觉精确控制的共享自主/视觉辅助融合；
3. 视觉对用户抖动、姿态误差和输入不精准的补偿效果；
4. 移动底盘与机械臂协同完成取物任务。

## 5. 推荐控制流程

1. 用户通过 IMU 控制底盘接近目标；
2. 视觉识别目标物体并估计其三维位置；
3. 用户选择目标并发出抓取指令；
4. 视觉控制接管机械臂末端的精确对准；
5. 机械臂完成抓取、抬升和递送。

IMU 适合提供粗略方向和操作意图，不建议依靠单个 IMU 长时间积分得到绝对位置。

## 6. 学习与开发顺序

### 阶段一：STM32 和单个 DM 电机

- GPIO、定时器、串口、CAN、中断、DMA；
- 阅读 DM4310、DM3519 手册；
- 搭建 CAN 通信；
- 实现电机使能、停止、位置/速度指令；
- 读取位置、速度、力矩和错误状态；
- 实现通信超时自动停止。

### 阶段二：一个角模块

- DM4310 单独完成转向角控制；
- DM3519 单独完成轮速控制；
- 标定转向零位、方向和机械限位；
- 转向与驱动组合控制。

### 阶段三：四角模块底盘

- 建立底盘逆运动学；
- 实现前进、横移、原地旋转和斜向运动；
- 标定四个模块的 ID、零位、方向、轮径和安装坐标；
- 测试速度误差、轨迹误差、承载和越障能力。

### 阶段四：机械臂

- 先确定自由度和负载；
- 计算各关节重力矩并选择 DM 电机；
- 先做单关节位置/速度闭环；
- 再做正运动学、逆运动学、末端控制和夹爪；
- 增加限位、防坠、急停和电流保护。

### 阶段五：IMU 和视觉集成

- IMU 零点校准、姿态融合、死区和滤波；
- 分时控制：底盘模式/机械臂模式；
- 与师妹约定目标坐标、坐标系和抓取接口；
- 最后再学习 ROS 2，接入系统通信和 MoveIt/Nav2。

## 7. 第一阶段最近目标

完成“STM32 + CAN + 一个 DM 电机控制实验”：

- 电机可以使能和停止；
- 能发送低速/小角度指令；
- 串口能显示位置、速度、力矩和错误码；
- 断开通信后电机自动停止；
- 形成可复用的 CAN 电机驱动代码。

## 8. 需要尽快确认的参数

- DM4310、DM3519 的准确型号、供电电压和协议手册；
- CAN 波特率和电机 ID 分配；
- 角模块转向范围、悬挂行程、轮径和模块安装坐标；
- 机械臂最大负载、臂长、自由度和各关节电机型号；
- STM32 主控型号和 CAN 收发器型号；
- 视觉输出使用相机坐标系还是机器人 base 坐标系。

## 9. 论文方向与双方具体工作

### 论文拟研究的问题

面向上肢运动能力受限用户在室内环境中的取物需求，设计一套移动机械臂辅助取物系统。用户通过前臂 IMU 对机器人进行低精度、分时遥操作：先控制角模块底盘接近目标，再控制机械臂进行大致靠近；视觉系统识别目标物体并估计其三维位置，自动修正机械臂末端位姿，最终完成抓取。

论文重点不是证明角模块结构本身具有原创性，而是研究：

> 当用户输入存在姿态误差、手臂抖动和控制精度不足时，视觉辅助能否提高移动机械臂的取物成功率并降低用户操作负担。

### 你负责的研究与实现内容

1. **角模块底盘控制**
   - 使用 STM32 控制 DM4310 转向电机和 DM3519 驱动电机；
   - 完成 CAN 通信、电机使能、反馈读取、位置/速度闭环和安全停止；
   - 建立四个独立转向驱动模块的底盘运动学；
   - 实现前进、横移、旋转和组合运动；
   - 测试底盘速度误差、轨迹误差、承载能力和运动稳定性。

2. **机械臂设计与控制**
   - 根据目标抓取范围、负载和安装空间确定机械臂自由度；
   - 选择 DM 系列电机并完成关节结构、传动和防坠设计；
   - 完成关节位置/速度控制、正运动学、逆运动学、末端控制和夹爪控制；
   - 实现关节限位、急停、通信超时和碰撞/过流保护。

3. **IMU 遥操作与系统接口**
   - 读取前臂 IMU 姿态并进行零点校准、滤波和死区处理；
   - 将用户动作映射为底盘速度指令或机械臂末端增量速度指令；
   - 设计底盘模式、机械臂模式和抓取确认的分时控制逻辑；
   - 向视觉模块提供机械臂状态、末端位姿和当前控制模式；
   - 接收视觉模块输出的目标位姿或精确对准指令，并执行机械臂动作。

### 师妹负责的研究与实现内容

1. **目标物体检测**
   - 使用 RGB/RGB-D 相机采集环境图像；
   - 使用 YOLO 等方法识别杯子、药盒、瓶子等目标物体；
   - 输出目标类别、检测框和置信度。

2. **目标三维定位**
   - 结合深度信息计算目标在相机坐标系中的三维位置；
   - 完成相机标定以及相机坐标系到机器人/机械臂坐标系的转换；
   - 根据目标形状和姿态生成可抓取的目标位姿或抓取点。

3. **视觉辅助对准与抓取**
   - 根据目标与机械臂末端之间的位置误差生成精细修正指令；
   - 在机械臂接近目标时逐步接管精确对准；
   - 判断目标是否位于可抓取范围内，并向控制系统发送抓取许可或失败状态。

### 双方共同完成的系统工作

- 约定 ROS 2 或串口/CAN 上层接口、消息格式和坐标系；
- 完成“IMU 控制底盘 → 视觉识别目标 → 机械臂视觉辅助抓取”的整机联调；
- 设计纯 IMU、IMU+视觉辅助等对照实验；
- 测量任务完成时间、抓取成功率、末端定位误差、输入延迟和用户操作次数；
- 分析视觉辅助对 IMU 噪声、用户抖动和姿态偏差的补偿效果。

### 论文拟定题目

中文：**面向上肢运动能力受限人群的视觉辅助穿戴式 IMU 移动机械臂取物系统**。

英文：**Vision-Assisted Wearable-IMU Teleoperation of a Mobile Manipulator for Assistive Object Retrieval**。

### 论文预期贡献

1. 构建一套基于角模块移动底盘、穿戴式 IMU 和视觉辅助机械臂的低成本辅助取物原型系统；
2. 提出一种“用户提供粗略意图、视觉完成精确修正”的分时共享控制流程；
3. 通过误差扰动和对照实验验证视觉辅助对抓取成功率、定位精度和用户操作负担的改善。

---

最后更新：2026-09-04
